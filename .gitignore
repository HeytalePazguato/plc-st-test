# Build outputs
/dist/
/build/
/out/
/target/

# Local Claude Code settings
.claude/

# The blueprint conventions doc is part of THIS template repo for reference,
# but new projects should not ship it. After "Use this template", run:
#   git rm BLUEPRINT.md && git commit -m "chore: remove template reference"
# This entry then ensures it doesn't get re-added by accident.
/BLUEPRINT.md

# Editor / IDE
.idea/
.vscode/
*.swp

# OS noise
.DS_Store
Thumbs.db

# Secrets
.env
.env.local
*.pem
*.key

# Private design docs (plan + anchor spec) — never published
/docs/secret/

# Node / TypeScript
node_modules/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
.eslintcache

# Test/coverage output
coverage/
